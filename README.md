## todos
[x] log the uploads in db
  uploaded file data (length, word count, the transcription), userid, 

  summery
    userid, result(the apenai's upload result), status?, the summery, 
[x] CRUDs for prompts

venv
pip install -r requrements.txt
sudo apt install ffmpeg -y